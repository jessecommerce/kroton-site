<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Croton — Chromatic Immersion AI</title>

    <!-- FONT -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&display=swap" rel="stylesheet">

    <style>
        body {
            margin: 0;
            background: #000000;
            font-family: 'Inter', sans-serif;
            color: white;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .container {
            text-align: center;
            width: 100%;
            max-width: 600px;
            padding: 20px;
        }

        /* GLOWING AURA */
        .aura {
            width: 220px;
            height: 220px;
            margin: 0 auto 40px auto;
            border-radius: 50%;
            background: radial-gradient(circle,
                rgba(255,255,255,0.3) 0%,
                rgba(255,0,150,0.3) 40%,
                rgba(0,200,255,0.25) 70%,
                rgba(0,0,0,0) 100%
            );
            filter: blur(20px);
            animation: pulse 5s infinite alternate ease-in-out;
        }

        @keyframes pulse {
            from { transform: scale(1); opacity: 0.8; }
            to { transform: scale(1.2); opacity: 1; }
        }

        /* LOGO */
        .logo {
            width: 130px;
            margin: -180px auto 40px auto;
            display: block;
            filter: drop-shadow(0 0 12px rgba(255,255,255,0.3));
        }

        /* INPUT BAR */
        .input-wrapper {
            background: rgba(255,255,255,0.08);
            border: 1px solid rgba(255,255,255,0.2);
            border-radius: 16px;
            display: flex;
            padding: 14px 18px;
            align-items: center;
            backdrop-filter: blur(8px);
        }

        input {
            flex-grow: 1;
            background: transparent;
            border: none;
            color: white;
            outline: none;
            font-size: 17px;
        }

        .send-btn {
            border: none;
            background: white;
            color: black;
            padding: 8px 18px;
            border-radius: 12px;
            font-weight: 600;
            cursor: pointer;
            transition: 0.2s;
        }

        .send-btn:hover {
            background: #dcdcdc;
        }

        .tagline {
            margin-top: 25px;
            opacity: 0.7;
            font-size: 14px;
        }
    </style>
</head>

<body>
    <div class="container">
        
        <!-- AURA BEHIND LOGO -->
        <div class="aura"></div>

        <!-- LOGO IMAGE -->
        <img src="logo.png" class="logo" alt="Croton Logo">

        <!-- INPUT FIELD -->
        <div class="input-wrapper">
            <input type="text" placeholder="Ask Croton anything..." />
            <button class="send-btn">→</button>
        </div>

        <!-- TAGLINE -->
        <div class="tagline">Design a life, not just a living.</div>
    </div>
</body>
</html>
